// Single source of truth for the "current" academic term, used when seeding
// or auto-creating fee invoices and grade records.
const CURRENT_TERM = 'Current Semester';

module.exports = { CURRENT_TERM };
